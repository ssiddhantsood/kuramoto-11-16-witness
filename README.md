# Finite Kuramoto witnesses above \(11/16\)

This repository contains compact, reproducible specifications of finite simple
unweighted graphs whose homogeneous Kuramoto model has a strict
nonsynchronous equilibrium.

## All finite records

The sorted catalog is in [`records/`](records/README.md). Each numeric folder
uses the form

```text
0.68875-reflection-n999962/
```

and contains a compact graph specification, standardized metadata, a verifier,
and the available interval, audit, and nonlinear certificates. Audit-pending
records are labeled explicitly.

## Current record: N=80,002

The current record is documented in
[`records/n80002/`](records/n80002/README.md).

- Vertices: \(N=80{,}002\)
- Edges: \(2{,}232{,}211{,}521\)
- Minimum degree: \(\delta=55{,}018\)
- Exact connectivity:

  \[
  \mu=\frac{\delta}{N-1}
  =\frac{55018}{80001}
  =0.687716403544955688\ldots
  >\frac{11}{16}.
  \]

The comparison is exact:

\[
16(55018)-11(80001)=277>0.
\]

At its reflection-paired six-class equilibrium:

- high-precision Newton refinement drives the torque residual below
  \(10^{-170}\) in the packaged verifier;
- an outward-rounded Krawczyk calculation certifies a unique root in the
  displayed short-seed box;
- the order parameter is approximately \(0.03329948\);
- the quotient Hessian has exactly one zero, generated by global rotation;
- the absolute quotient gap is approximately \(0.4268309591\);
- the normalized gap is approximately \(5.335253608\times10^{-6}\), with an
  independent rigorous lower bound above \(5.33507\times10^{-6}\);
- a Fourier-free biregular operator argument proves all \(79{,}996\)
  transverse modes positive.

The independent audit returned `CONFIRMED`. Nonlinear return is local: tested
weak-mode amplitudes \(10^{-4}\) and \(3\times10^{-4}\) returned, while
\(5\times10^{-4}\) and \(2\times10^{-3}\) escaped to synchrony. No global
basin claim is made.

Current-record files:

- [`records/n80002/graph_spec.json`](records/n80002/graph_spec.json) — exact
  graph and phase specification.
- [`records/n80002/verify.py`](records/n80002/verify.py) — standalone verifier
  reading only the adjacent graph specification.
- [`records/n80002/verification_report.json`](records/n80002/verification_report.json)
  — generated verifier output.
- [`records/n80002/audit_verdict.json`](records/n80002/audit_verdict.json) —
  machine-readable independent verdict and qualifications.
- [`records/n80002/README.md`](records/n80002/README.md) — construction and
  proof details.

## First witness: N=460,800

The original repository-root artifacts remain the first rigorously confirmed
witness and are intentionally unchanged:

- [`graph_spec.json`](graph_spec.json) — original \(N=460{,}800\) graph.
- [`verify_witness.py`](verify_witness.py) — original standalone verifier.
- [`verification_report.json`](verification_report.json) — original verifier
  output.
- [`INDEPENDENT_AUDIT.md`](INDEPENDENT_AUDIT.md) — original adversarial audit.
- [`audit_verdict.json`](audit_verdict.json) — original audit verdict.

Its exact ratio is

\[
\frac{316802}{460799}
=0.6875058322609207\ldots
>\frac{11}{16},
\]

with cross-product excess 43, absolute Hessian gap approximately 48.74396,
and normalized gap approximately \(1.0578\times10^{-4}\).

The current record improves the exact ratio by

\[
\frac{55018}{80001}-\frac{316802}{460799}
=\frac{7762580}{36864380799}>0
\]

while reducing the vertex count by approximately \(82.64\%\).

## Formal certificate

The existing formal certificate and verifier are retained unchanged in
[`formal/`](formal/README.md).

## Verify

Install the existing dependencies once:

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

Verify the current record:

```bash
python3 records/n80002/verify.py
```

Verify the first witness:

```bash
python3 verify_witness.py --out verification_report.json
```

Both graphs use six clique fibers. A partial cross block between classes
\(i<j\) is defined by the exact rule

```text
(v - u - shift_start) mod gcd(n_i, n_j) < shift_count
```

for local labels `u` and `v`. This gives a compact \(O(1)\) adjacency oracle;
explicit multi-billion-edge lists are unnecessary.

## Why the transverse bounds are complete

For either witness, the equitable six-class partition splits the Hessian into
a six-dimensional class-constant quotient and the direct sum of zero-sum
subspaces inside the fibers.

On the latter space:

- a clique on fiber \(i\) contributes \(n_i I\);
- a complete cross block vanishes;
- a partial \((d_{ij},d_{ji})\)-biregular block has adjacency norm at most
  \(\sqrt{d_{ij}d_{ji}}\).

Each verifier assembles the resulting six-class comparison matrix. Positivity
of that matrix proves positivity of every transverse direction without
materializing the graph or relying on sampled modes.
